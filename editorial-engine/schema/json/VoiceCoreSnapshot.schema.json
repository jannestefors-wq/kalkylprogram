{
  "$defs": {
    "Actor": {
      "description": "Who or what produced a piece of data.",
      "enum": [
        "human",
        "ai_system",
        "import"
      ],
      "title": "Actor",
      "type": "string"
    },
    "EvidenceCertainty": {
      "description": "How solid is a given piece of editorial data.\n\nMirrors Beslut 23's required three-way split, plus an explicit\n\"unconfirmed\" state so that uncertainty is a first-class value rather\nthan an absent field or a disguised default.",
      "enum": [
        "verified",
        "strongly_supported",
        "analytical_proposal",
        "unconfirmed"
      ],
      "title": "EvidenceCertainty",
      "type": "string"
    },
    "Provenance": {
      "additionalProperties": false,
      "properties": {
        "created_by": {
          "$ref": "#/$defs/Actor"
        },
        "actor_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Free identifier for the actor, e.g. a human's name/initials or an AI system+model name. None only if genuinely unrecorded.",
          "title": "Actor Id"
        },
        "created_at": {
          "format": "date-time",
          "title": "Created At",
          "type": "string"
        },
        "certainty": {
          "$ref": "#/$defs/EvidenceCertainty"
        },
        "method": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How the value was produced, e.g. 'manual_entry', 'fas_0a_analysis', 'fas_0b_analysis', 'ai_inference'.",
          "title": "Method"
        },
        "analysis_logic_version": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Version identifier of whatever analysis logic produced an interpretation, distinct from schema_version. REQUIRED (validated below) whenever created_by == Actor.AI_SYSTEM. May be null for human-created provenance.",
          "title": "Analysis Logic Version"
        },
        "supporting_source_ids": {
          "description": "SOURCE.source_id values that back this claim, if any. Empty list, not a guess, when nothing supports it yet.",
          "items": {
            "type": "string"
          },
          "title": "Supporting Source Ids",
          "type": "array"
        },
        "schema_version": {
          "default": "1.0.0",
          "title": "Schema Version",
          "type": "string"
        },
        "notes": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notes"
        }
      },
      "required": [
        "created_by",
        "created_at",
        "certainty"
      ],
      "title": "Provenance",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "V1.1, OQ-1 resolution: a free-text label like \"voice-core-1.0\" was\njudged insufficient long-term -- we must later be able to state exactly\nwhich VoicePrinciple rows a text or analysis was judged against, not\njust which label was in fashion at the time.\n\nDesign choice (documented per the order's explicit request): this\nREFERENCES existing `VoicePrinciple` rows by id\n(`voice_principle_ids`) rather than duplicating their `definition`/\n`anti_definition` text. A principle's canonical wording has exactly one\nhome (`VoicePrinciple`); duplicating it into every snapshot that ever\nincluded it would recreate the Adam-style two-representations-that-\ndrift-apart problem this schema exists to avoid (Beslut 27). Looking up\n\"what did principle X actually say under snapshot Y\" is a one-hop join\n(`VoicePrinciple.version`/`valid_from` already answers \"as of when\"),\nwhich is cheap enough that duplication buys nothing.\n\nLifecycle: reuses the exact same `active` + `superseded_by` pattern\nalready used by `VoicePrinciple` and the other registries, rather than\nintroducing a new status enum -- see docs/TECHNICAL_PROPOSALS.md TP-8\nand docs/OPEN_QUESTIONS.md (Beslut 9 of the V1.1 order: no parallel\nstatus model unless the lifecycle genuinely differs, and a snapshot's\ndoes not).",
  "properties": {
    "snapshot_id": {
      "title": "Snapshot Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.0.0",
      "title": "Schema Version",
      "type": "string"
    },
    "version": {
      "description": "e.g. '1.0' -- what ContentRecord/QualityAssessment refer to as the Voice Core version used.",
      "title": "Version",
      "type": "string"
    },
    "created_at": {
      "format": "date-time",
      "title": "Created At",
      "type": "string"
    },
    "voice_principle_ids": {
      "description": "FK -> VoicePrinciple.voice_principle_id. The exact set of principles in effect at this snapshot.",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Voice Principle Ids",
      "type": "array"
    },
    "active": {
      "default": true,
      "title": "Active",
      "type": "boolean"
    },
    "superseded_by": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "snapshot_id of the snapshot that replaced this one as the current reference, if any.",
      "title": "Superseded By"
    },
    "provenance": {
      "$ref": "#/$defs/Provenance"
    },
    "notes": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Notes"
    }
  },
  "required": [
    "snapshot_id",
    "version",
    "created_at",
    "voice_principle_ids",
    "provenance"
  ],
  "title": "VoiceCoreSnapshot",
  "type": "object"
}
